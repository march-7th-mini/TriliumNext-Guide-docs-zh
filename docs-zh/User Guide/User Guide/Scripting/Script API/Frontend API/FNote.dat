好的，这是您要求的 Markdown 文档的简体中文翻译：

---

# 在 Trilium 中创建自定义小组件

Trilium 提供了一种通过自定义小组件来扩展其用户界面的强大方式。本指南将引导您完成创建第一个自定义小组件的步骤。

## 什么是自定义小组件？

自定义小组件是使用 HTML、CSS 和 JavaScript 构建的，可以放置在 Trilium 界面各个部分（例如侧边栏、标题栏或作为笔记内容）的交互式元素。它们允许您添加个性化功能、集成外部服务或创建自定义工具。

## 创建您的第一个小组件

请按照以下步骤创建一个简单的小组件，该小组件将显示一个问候消息和一个按钮。

### 第 1 步：创建小组件笔记

1.  在笔记树中，导航到您想要存储小组件的位置（例如，在 `_自定义` 文件夹下）。
2.  右键点击并选择 **新建笔记**。
3.  将笔记命名为 `问候小组件`。
4.  在笔记类型下拉菜单中，选择 **自定义小组件**。
5.  点击 **确定** 创建笔记。

### 第 2 步：编写小组件代码

现在，您将向小组件笔记中添加 HTML、CSS 和 JavaScript 代码。

1.  打开新创建的 `问候小组件` 笔记。
2.  在笔记编辑器中，点击 **代码** 图标（`</>`）切换到代码视图。
3.  将以下代码复制并粘贴到编辑器中：

```html
<div id="greeting-widget">
  <style>
    #greeting-widget {
      font-family: sans-serif;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 5px;
      background-color: #f9f9f9;
    }
    #greeting-widget button {
      background-color: #4CAF50;
      color: white;
      padding: 10px 15px;
      border: none;
      border-radius: 3px;
      cursor: pointer;
      margin-top: 10px;
    }
  </style>
  <p id="greeting-text">你好，世界！</p>
  <button id="greeting-button">点击我</button>

  <script>
    document.getElementById('greeting-button').addEventListener('click', function() {
      document.getElementById('greeting-text').textContent = '你好，Trilium！';
    });
  </script>
</div>
```

### 第 3 步：保存并查看小组件

1.  点击 **保存** 图标或按 `Ctrl+S` (Windows/Linux) 或 `Cmd+S` (Mac) 保存笔记。
2.  退出代码视图（点击 **代码** 图标切换回来）。
3.  您应该会在笔记编辑器中看到小组件的预览。点击 **点击我** 按钮，文本应该会改变。

## 将小组件放置在界面中

您创建的小组件目前仅在您打开其笔记时可见。要将其放置在界面的其他部分，您需要使用 **小组件** 功能。

1.  在笔记树中，右键点击您的 `问候小组件` 笔记。
2.  选择 **小组件**。
3.  在子菜单中，选择您想要放置小组件的位置。选项包括：
    *   **仪表板**：将小组件添加到 Trilium 的仪表板页面。
    *   **侧边栏**：将小组件添加到左侧边栏。
    *   **标题栏**：将小组件添加到顶部标题栏。
    *   **自定义**：允许您指定一个自定义位置。

选择后，小组件将立即出现在界面的指定位置。

## 小组件类型

Trilium 支持两种主要类型的小组件：

*   **内置小组件**：这些是 Trilium 自带的，提供核心功能，例如 `反链`、`属性` 和 `可用启动器`。
*   **自定义小组件**：这些是您自己创建的，用于添加自定义功能和集成。

## 高级主题

*   **访问 Trilium API**：您可以在自定义小组件中使用 Trilium 的 API 与笔记、属性和其他数据进行交互。这为创建强大的工具和集成打开了无限可能。
*   **使用 `api` 对象**：在小组件的 JavaScript 代码中，您可以通过 `api` 对象访问 Trilium API。例如，`api.currentNote()` 返回当前笔记对象。
*   **动态内容**：您可以使用 JavaScript 根据用户交互或其他事件动态更新小组件的内容。

## 故障排除

*   **小组件不显示**：确保您已正确保存笔记，并且已通过 **小组件** 菜单将其放置在所需位置。
*   **JavaScript 错误**：打开浏览器的开发者控制台（通常按 `F12`）查看是否有任何 JavaScript 错误。这些错误会提供有关问题所在的有用信息。
*   **样式问题**：检查您的 CSS 选择器是否与小组件中的 HTML 结构匹配。使用浏览器的检查器（右键点击元素并选择“检查”）来调试样式。

## 结论

自定义小组件是 Trilium 中一个非常强大的功能，允许您根据自己的特定需求定制界面。通过结合 HTML、CSS、JavaScript 和 Trilium API，您可以创建几乎任何您能想象到的东西。祝您编码愉快！