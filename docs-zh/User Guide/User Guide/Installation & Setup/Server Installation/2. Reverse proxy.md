# 2. 反向代理

Trilium 通常通过**反向代理**暴露到互联网，该代理负责 TLS 终止、提供单一公共入口点，并且（可选）允许您在一个域名下托管多个实例。Trilium 本身在其本地端口上通过纯 HTTP 运行；代理位于其前面。

本节收集了特定于提供商的指南以及无论您使用哪个代理都适用的配置。

## 提供商指南

*   <a class="reference-link" href="2.%20Reverse%20proxy/Nginx.md">Nginx</a>
*   <a class="reference-link" href="2.%20Reverse%20proxy/Traefik.md">Traefik</a>
*   <a class="reference-link" href="2.%20Reverse%20proxy/Apache%20using%20Docker.md">Apache using Docker</a>

## 适用于每个代理

无论您选择哪个代理，请确保以下事项：

*   **转发 WebSocket 升级。** Trilium 使用 WebSocket 进行同步和实时 UI 更新。您的代理必须传递 `Upgrade` 和 `Connection` 头，否则客户端看起来已加载但永远不会收到更新。
*   **配置受信任的代理。** 以便 Trilium 看到真实的客户端 IP（用于速率限制、日志记录和安全 Cookie），而不是代理的地址。参见 <a class="reference-link" href="2.%20Reverse%20proxy/Trusted%20proxy.md">受信任的代理</a>。
*   **在代理处终止 TLS。** 参见 <a class="reference-link" href="HTTPS%20(TLS).md">HTTPS (TLS)</a>。

## 在 Trilium 前面放置身份验证代理

某些设置会在代理处放置一个**前向认证 / SSO 层**（Authelia、Authentik、oauth2-proxy 或 OIDC 插件），以便_每个_请求都必须先通过交互式登录。

这**会破坏同步和初始设置。** 同步是一个机器对机器的过程，在后台无人值守地运行——它没有浏览器，无法完成交互式登录挑战。当代理拦截同步请求时，它返回其 HTML 登录页面而不是预期的 JSON，客户端会失败并出现如下错误：

```
Sync failed: Request to GET https://trilium.example.com/api/setup/status failed,
error: Unexpected token '<', "<!doctype "... is not valid JSON

```

有两种方法可以解决此问题：

*   **首选，使用 Trilium 的内置身份验证。** 根本不要在代理处对 Trilium 进行网关控制。Trilium 可以自行对 Web 界面进行身份验证，包括通过 [OpenID Connect](Signing%20in%20with%20OpenID%20Connect.md)。这使同步端点可以按照其设计方式进行身份验证。
*   **将同步端点从代理的身份验证中豁免。** 如果您必须在代理处保留身份验证，请将这些路径从前向认证检查中排除：
    *   `/api/setup/*`
    *   `/api/login/sync`
    *   `/api/sync/*`

> [!NOTE]
> 这是安全的：Trilium 会自行对这些端点进行身份验证。同步使用基于共享密钥的无挑战 HMAC 握手，并且一次性设置种子请求受密码保护——因此在代理处豁免它们不会暴露对您数据的未授权访问。

一个 Nginx 示例，其中 `/auth` 是您的前向认证端点：

```
server {
    # ... TLS、server_name 等 ...

    # 同步和设置端点自行进行身份验证 — 跳过前向认证。
    location ~ ^/api/(sync|login/sync|setup) {
        auth_request off;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_pass http://trilium;
    }

    # 所有其他请求都经过前向认证。
    location / {
        auth_request /auth;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_pass http://trilium;
    }
}
```